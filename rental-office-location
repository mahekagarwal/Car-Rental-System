SQL> CREATE TABLE RENTAL_OFFICE_LOCATION
  (
  RentalOfficeID INT NOT NULL,
  StreetAddress VARCHAR(200) NOT NULL,
  City VARCHAR(100) NOT NULL,
  State VARCHAR(100) NOT NULL,
  ZipCode INT NOT NULL,
  PRIMARY KEY (RentalOfficeID)
  );
